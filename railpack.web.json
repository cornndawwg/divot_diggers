{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": [
        "pnpm build:web"
      ]
    }
  },
  "deploy": {
    "startCommand": "pnpm start:web"
  }
}
