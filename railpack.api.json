{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": [
        "pnpm typecheck"
      ]
    }
  },
  "deploy": {
    "startCommand": "pnpm release && pnpm start:api"
  }
}
