{
  "name": "@ddga/scoring-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure scoring functions. No I/O, no dates, no React, no network. Zero runtime dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@ddga/types": "workspace:*"
  }
}
