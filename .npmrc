# React Native's tooling assumes a flat node_modules and reaches for files that packages do
# not list in their "exports" — Metro fails on react-native/rn-get-polyfills otherwise. This
# is the layout Expo documents for pnpm workspaces.
node-linker=hoisted
